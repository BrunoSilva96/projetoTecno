export const uploadService = {
  uploadCreate: async (
    fileName: string,
    postId: string,
    activitiesId: string
  ) => {
    /* Parte que falta para resolver e completar a atividade, funcionando da pra testar os relacionamentos e a funcionalidade total do código*/
  },
};
